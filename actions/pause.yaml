---
name: "pause"
runner_type: "run-python"
description: "Pause Sonos"
enabled: true
entry_point: "pause.py"
parameters:
  ip:
    type: "string"
    description: "IP address of Sonos device to send command to"
    required: true
